object Plugin {
    object Name {
        object Spring {
            val boot = "org.springframework.boot"
            val dependecyManagement = "io.spring.dependency-management"
        }

        object Kotlin {
            val jvm = "jvm"
            val spring = "plugin.spring"
            val serialization = "plugin.serialization"
        }
    }
}
